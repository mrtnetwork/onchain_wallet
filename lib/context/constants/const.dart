class AppContextConst {
  static const Duration defaultConnectionRequestTimeout = Duration(minutes: 2);
  static const Duration nativeCreateConnectionTimeount = Duration(seconds: 60);

  static const Duration databaseOperationTimeout = Duration(seconds: 30);
  static const Duration createConnectionTimeout = Duration(seconds: 60);
  static const Duration spawnIsolateTimeout = Duration(seconds: 90);
  static const Duration utilsOperationTimeout = Duration(seconds: 90);
}
